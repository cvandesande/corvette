# Agentic Pipeline Playbook

The canonical, tool-neutral pipeline is maintained under:

`/home/cvandesande/.config/ai-agent/standards/`

Load only what your role requires:

- orchestrator: `AGENTIC_PIPELINE_PLAYBOOK.md` and `roles/ORCHESTRATOR.md`;
- implementer: `roles/IMPLEMENTER.md` and, for local Qwen, `LOCAL_MODEL_PROFILES.md`;
- reviewer: `roles/REVIEWER.md`;
- researcher: `roles/RESEARCHER.md`.

Also read this repository’s `AGENTS.md`, required standards, and approved task artifacts. Do not copy the canonical documents into prompts: briefs contain only approved task-specific facts and use the canonical templates.